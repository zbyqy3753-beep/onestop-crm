/**
 * אין נתוני זרע — המערכת מתחילה ריקה לגמרי.
 *
 * הקובץ נשאר כנקודת כניסה ל-`npx prisma db seed` למקרה שבעתיד תרצה
 * להזין נתונים התחלתיים ל-MySQL. כרגע הוא לא כותב כלום.
 */
async function main() {
  console.log("אין נתוני זרע להזנה — המערכת מתחילה ריקה.");
}

main();
