import type { CapacitorConfig } from "@capacitor/cli";

/**
 * מעטפת האפליקציה הנייטיבית.
 *
 * ⚠️ **האפליקציה לא מכילה את הקוד של המערכת — היא טוענת אותו מהשרת.**
 * זו החלטה מרכזית ולא ברירת מחדל: ה-CRM משתנה תכופות, וכל תיקון היה
 * דורש בנייה מחדש, העלאה לחנות, המתנה לאישור, ואז תקווה שכל עובד
 * יעדכן. עם `server.url` תיקון שנפרס ל-Vercel מגיע לכל הטלפונים
 * בשנייה שאחריה, והאפליקציה בחנות נשארת יציבה לחלוטין.
 *
 * המחיר: בלי רשת אין עבודה. זה זהה למצב היום באתר, ומקובל ל-CRM
 * שממילא כל נתוניו חיים בשרת. `public/sw.js` מגיש את `offline.html`
 * כשאין חיבור.
 */
const config: CapacitorConfig = {
  /*
   * ⚠️ מזהה החבילה הוא **קבוע לנצח**. הוא הזהות של האפליקציה בחנות,
   * ושינוי שלו אחרי הפרסום יוצר אפליקציה חדשה לגמרי — לא עדכון.
   * המשתמשים היו נשארים עם הישנה מותקנת ובלי שום רמז.
   */
  appId: "il.co.onestop.crm",

  // מה שמופיע מתחת לאייקון במסך הבית. עד ~12 תווים לפני שנחתך.
  appName: "ONE STOP",

  /*
   * חובה שיהיה קיים גם כשלא משתמשים בו: Capacitor מצפה לתיקיית נכסי
   * ווב גם במצב `server.url`. `public/` כבר קיימת ומכילה את
   * `offline.html` ואת האייקונים.
   */
  webDir: "public",

  server: {
    /*
     * ⚠️ הכתובת נצרבת ל-APK. שינוי שלה מחייב בנייה והפצה מחדש, ולכן
     * כשיתחבר הדומיין האמיתי צריך לזכור להחליף כאן ולבנות שוב.
     *
     * חייב להיות HTTPS: עוגיית הסשן היא `secure`, ובלי HTTPS
     * הדפדפן המוטמע פשוט לא ישמור אותה והמשתמש לא יוכל להתחבר.
     */
    url: "https://onestop-crm-demo.vercel.app",

    /*
     * `false` במפורש. ברירת המחדל חוסמת HTTP רגיל — וזה מה שאנחנו
     * רוצים, אבל עדיף שזה יהיה כתוב ולא יסתמך על ברירת מחדל.
     */
    cleartext: false,

    /*
     * ⚠️ הדומיין חייב להיות ברשימה, אחרת ניווט אליו נפתח בדפדפן
     * החיצוני במקום בתוך האפליקציה — והמשתמש היה מאבד את הסשן
     * (מחיצת אחסון נפרדת) ורואה סרגל כתובת.
     */
    allowNavigation: ["onestop-crm-demo.vercel.app"],
  },

  android: {
    /*
     * הרקע שמאחורי ה-WebView לפני שהדף נצבע. זהה ל-`background_color`
     * במניפסט ול-`--c-bg` בתמה הכהה — בלי זה יש הבזק לבן בכל פתיחה.
     */
    backgroundColor: "#081421",
  },
};

export default config;
