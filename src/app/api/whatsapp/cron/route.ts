import { NextResponse } from "next/server";
import { drainOutbox } from "@/server/whatsapp/drain";

/**
 * השעון של התור — מנקז את מה שבשל לשליחה.
 *
 * ⚠️⚠️ **בלי הנתיב הזה תזכורות פשוט לא יוצאות בזמן.** `drainOutbox`
 * נקרא רק ממקום אחד נוסף — ה-webhook — כלומר רק כשלקוח כותב לנו.
 * בגרסת הבוט הלא רשמי הסקר כל 20 שניות היה השעון; משהוא הוסר, תזכורת
 * שנקבעה ל-16:00 הייתה יושבת בתור עד שמישהו במקרה שולח הודעה.
 *
 * ⚠️ הניקוז ב-webhook **נשאר** ואינו מיותר: תשובה של לקוח מייצרת
 * הודעת אישור שיוצאת באותה בקשה, תוך שברירי שנייה. ה-cron מכסה את
 * מה שאין לו טריגר — לא מחליף אותו.
 *
 * ⚠️ **התזמון עצמו יושב ב-`vercel.json` שבשורש, וזה קובץ שקל לא לשים
 * לב שחסר.** הוא נעדר מהמאגר מיום הקמתו ועד 18.8.2026, ולכן הנתיב
 * הזה — שקיים בדיוק כדי להיות השעון — לא נקרא אף פעם. התוצאה לא
 * נראתה כמו תקלה: הודעות כן יצאו, כי ה-webhook מנקז את התור בכל
 * פעם שלקוח כותב לנו. תזכורת יצאה רק אם במקרה מישהו הודיע משהו
 * בסמוך לשעה שלה, ואחרת נשארה בתור. `BotStatus` הראה "הבוט לא נראה
 * כבר X" — האזהרה עבדה, רק שאיש לא ידע שהיא מצביעה על קובץ חסר.
 *
 * ⚠️⚠️ **מי שמפעיל את הנתיב הזה הוא `pg_cron` שב-Supabase, לא Vercel.**
 *
 * חשבון Vercel מסוג Hobby מרשה cron יומי בלבד — ביטוי של כל חמש
 * דקות נדחה בפריסה עצמה. לכן ה-cron ב-`vercel.json` נשאר כרשת ביטחון
 * יומית בלבד, והתזמון האמיתי יושב במסד:
 *
 *     select * from cron.job where jobname = 'onestop-drain';
 *
 * ⚠️⚠️ **התדירות שם היא כל שתי דקות בשעות 4–19 ב-UTC
 * (הביטוי המדויק בהערת השורה שמתחת), ושני החלקים מכוונים.**
 *
 * **המרווח הוא הדיוק של התזכורת.** מילוי התור קורה רק בתקתוק
 * (`enqueueDueFollowUps` נקרא מתוך `pull`), ולכן חזרה שנקבעה ל-10:31
 * לא נכנסה לתור ב-10:20 בתקתוק של חמש דקות — היא נכנסה ב-10:25,
 * ההקדמה של עשר דקות הצטמצמה לשש, ובנוסח הישן ההודעה אף הכריזה על
 * עצמה "באיחור". חזרה בשעה עגולה נפלה על תקתוק ויצאה נכון, ולכן זה
 * נראה אקראי. הרצה כפולה בטוחה מעצם הבנייה (התביעה היא `updateMany`
 * מותנה על `status: "queued"`), ולכן קיצור המרווח אינו מסתכן בכפילות.
 *
 * ⚠️ **טווח השעות אינו קישוט — הוא מכסת ה-CPU.** מחוץ לחלון השליחה
 * `claim` חוסם הכול, כלומר תקתוק לילי מעיר את הפונקציה כדי לא לשלוח
 * כלום. ב-Hobby זה נמדד: הניקוז חוצה חמש שניות לקריאה, ומכסת
 * `Fluid Active CPU` היא 4 שעות לחודש. תקתוק 24/7 כל דקה שרף אותה
 * בקצב שלא מחזיק חודש.
 *
 * ⚠️ **השעות ב-UTC, כי `cron.timezone` הוא GMT** — לא שעון ישראל.
 * חלון 08:00–21:00 בישראל הוא 05:00–18:00 בקיץ ו-06:00–19:00 בחורף,
 * ו-`4-19` מכסה את שניהם. אל תצמצם אותו ל-`5-17` "כדי לדייק": זה
 * נכון בקיץ, ובחורף התזכורות של 19:00–21:00 פשוט לא יוצאות.
 *
 * ⚠️ הסוד שהוא שולח הוא `CRON_SECRET`, והוא כתוב **בשני מקומות** —
 * במשתני הסביבה של Vercel ובהגדרת ה-job במסד. החלפה באחד בלי השני
 * מחזירה 401 בשקט, וההיסטוריה של המתזמן לא תסביר למה.
 *
 * אימות: `x-api-key` מול `WHATSAPP_API_KEYS`,
 * אותו רישום כמו `/pull` ו-`/report` — הנתיב פתוח בשער הגישה
 * (`PUBLIC_PREFIXES` ב-`proxy.ts`) ולכן חייב לאמת את עצמו.
 */

// הביטוי כפי שהוא רשום ב-`cron.job`:  */2 4-19 * * *
// ⚠️ בהערת שורה ולא בהערת בלוק, מסיבה ממשית: שני התווים
// שפותחים אותו **סוגרים הערת בלוק**, וכתיבתו למעלה שברה את הקובץ.

/** מפתח ה-cron של Vercel, כשהקריאה מגיעה מהתשתית ולא מהבוט. */
function fromVercelCron(request: Request): boolean {
  const secret = process.env.CRON_SECRET?.trim();
  if (!secret) return false;

  const header = request.headers.get("authorization");
  return header === `Bearer ${secret}`;
}

async function run(request: Request): Promise<Response> {
  const { botFromKey } = await import("@/server/auth/apiKeys");

  const allowed =
    fromVercelCron(request) || Boolean(botFromKey(request.headers.get("x-api-key")));

  if (!allowed) {
    return NextResponse.json(
      { success: false, error: "אימות נכשל" },
      { status: 401 },
    );
  }

  const result = await drainOutbox(new URL(request.url).origin);
  return NextResponse.json({ success: true, ...result });
}

/**
 * GET ולא POST: כך Vercel Cron קורא, וכך גם אפשר לבדוק ידנית עם
 * curl בלי גוף בקשה. אין כאן שום דבר שנקרא — רק תופעת לוואי — ולכן
 * זו חריגה מודעת מהסמנטיקה הרגילה של GET.
 */
export async function GET(request: Request) {
  return run(request);
}

export async function POST(request: Request) {
  return run(request);
}
